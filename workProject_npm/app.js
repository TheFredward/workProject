// if needed can demonstrate using an a database and local server
const express = require('express');
const bodyPars = require('body-parser');
const mongoose = require('mongoose');
const app = express();
